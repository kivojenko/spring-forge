# Version 0.1.19 (2026-08-07)

### Bug Fixes
- **Custom ID Filtering**: Fixed an issue where filtering by associations would fail if the associated entity used a custom ID name instead of the default `id`. The filtering logic now correctly identifies and uses the actual JPA ID property name.
